# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: E2E CDS shared build integration

# Trigger only on pushes to main or copy-pr-bot mirror branches, and only when relevant paths change
on:
  push:
    branches:
      - e2e/72-cds-publish
      - e2e/72-cds-local
    paths:
      - 'cds-containers/**'
      - '.github/workflows/build-cds-containers.yml'
      - '.github/actions/docker-build/**'
      - '.github/workflows/attest-image.yml'
      - '.github/scripts/prepare-image-attestation.sh'
      - '.github/scripts/promote-attested-image.sh'
      - '.github/scripts/check_cds_tags.py'
  workflow_dispatch:

concurrency:
  group: build-cds-containers-${{ github.ref }}
  # Keep pending releases; the default single pending slot replaces older runs.
  queue: max
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAMESPACE: lachen-nv
  IMAGE_PREFIX: dsx-attest-e2e-  # Fork-only test packages, never production CDS images

defaults:
  run:
    shell: bash

jobs:
  get-version:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      version: ${{ steps.extract-version.outputs.version }}
      publish: ${{ steps.extract-version.outputs.publish }}

    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0

      - name: Select fork-only E2E mode
        id: extract-version
        run: |
          set -euo pipefail
          [[ "$GITHUB_REPOSITORY" == lachen-nv/dsx-github-actions ]]
          version="0.0.$GITHUB_RUN_ID"
          publish=false
          if [[ "$GITHUB_REF" == refs/heads/e2e/72-cds-publish ]]; then
            publish=true
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "publish=$publish" >> "$GITHUB_OUTPUT"
          echo "Container version: $version (publish: $publish)"

  build-and-test-images:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write  # Required to push to GHCR
    needs: get-version
    strategy:
      fail-fast: false
      matrix:
        image:
          - name: go-dev-1.24-alpine
            path: cds-containers/go-dev-1.24-alpine
            description: "Go 1.24 development container (Alpine-based, minimal size)"

    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5.10.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ env.IMAGE_PREFIX }}${{ matrix.image.name }}
          tags: |
            type=raw,value=build-${{ github.run_id }}-${{ github.run_attempt }}
          flavor: latest=false
          labels: |
            org.opencontainers.image.description=${{ matrix.image.description }}
            org.opencontainers.image.vendor=NVIDIA
            org.opencontainers.image.version=${{ needs.get-version.outputs.version }}

      - name: Build image for testing
        id: build
        uses: ./.github/actions/docker-build
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ env.IMAGE_PREFIX }}${{ matrix.image.name }}
          context: ./cds-containers
          dockerfile: ${{ matrix.image.path }}/Dockerfile
          platforms: linux/amd64
          push: ${{ needs.get-version.outputs.publish }}
          load: ${{ needs.get-version.outputs.publish != 'true' && 'true' || 'false' }}
          prepare-attestation: ${{ needs.get-version.outputs.publish }}
          security-scan-enabled: "false"
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          buildkit-config: ""
          cache-scope: attest-e2e-cds-go-amd64

      - name: Select the exact built image
        id: test-image
        env:
          PUBLISH: ${{ needs.get-version.outputs.publish }}
          IMAGE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ env.IMAGE_PREFIX }}${{ matrix.image.name }}
          DIGEST: ${{ steps.build.outputs.digest }}
          IMAGE_ID: ${{ steps.build.outputs.imageid }}
        run: |
          if [[ "$PUBLISH" == "true" ]]; then
            [[ "$DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]
            echo "image=$IMAGE@$DIGEST" >> "$GITHUB_OUTPUT"
          else
            [[ "$IMAGE_ID" =~ ^sha256:[0-9a-f]{64}$ ]]
            echo "image=$IMAGE_ID" >> "$GITHUB_OUTPUT"
          fi

      - name: Test Go development image
        if: startsWith(matrix.image.name, 'go-dev-')
        env:
          IMAGE: ${{ steps.test-image.outputs.image }}
        run: |
          docker run --rm -i "$IMAGE" sh -ec '
            go version
            golangci-lint --version
            workdir="$(mktemp -d)"
            cd "$workdir"
            cat > hello.go
            goimports -w hello.go
            go build hello.go
            ./hello
          ' <<'EOF'
          package main
          func main() {
              fmt.Println("Hello from the CDS Go container")
          }
          EOF

      - name: Test tools image
        if: matrix.image.name == 'tools'
        env:
          IMAGE: ${{ steps.test-image.outputs.image }}
        run: |
          docker run --rm "$IMAGE" sh -ec '
            bazel --version
            bazel6 --version
            bazel8 --version
            kubectl version --client
            helm version --short
            terraform version
            terragrunt --version
            ngc version
            yq --version
            node --version
            python3 --version
            uv --version
          '

      - name: Record tested digest and evidence ID
        if: needs.get-version.outputs.publish == 'true'
        env:
          IMAGE_NAME: ${{ matrix.image.name }}
          DIGEST: ${{ steps.build.outputs.digest }}
          SBOM_ARTIFACT_ID: ${{ steps.build.outputs.sbom-artifact-id }}
        run: |
          [[ "$SBOM_ARTIFACT_ID" =~ ^[1-9][0-9]*$ ]]
          mkdir -p digests
          jq -n --arg image "$REGISTRY/$IMAGE_NAMESPACE/$IMAGE_PREFIX$IMAGE_NAME" \
            --arg digest "$DIGEST" --arg sbom_artifact_id "$SBOM_ARTIFACT_ID" \
            '{image: $image, digest: $digest, sbom_artifact_id: $sbom_artifact_id}' \
            > "digests/$IMAGE_NAME.json"

      - name: Upload tested digest
        if: needs.get-version.outputs.publish == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: cds-digest-${{ matrix.image.name }}
          path: digests/${{ matrix.image.name }}.json
          if-no-files-found: error
          retention-days: 1
          overwrite: true

  prepare-attestations:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    needs: [get-version, build-and-test-images]
    if: needs.get-version.outputs.publish == 'true'
    outputs:
      matrix: ${{ steps.subjects.outputs.matrix }}
    steps:
      - name: Download tested image metadata
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: cds-digest-*
          path: digests
          merge-multiple: true

      - name: Prepare the shared attestation matrix
        id: subjects
        run: |
          matrix="$(jq -sc '{include: .}' digests/go-dev-1.24-alpine.json)"
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  attest-images:
    needs: prepare-attestations
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.prepare-attestations.outputs.matrix) }}
    uses: ./.github/workflows/attest-image.yml
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    with:
      image: ${{ matrix.image }}
      digest: ${{ matrix.digest }}
      platforms: linux/amd64
      sbom_artifact_id: ${{ matrix.sbom_artifact_id }}
      registry: ghcr.io
      runner: ubuntu-latest
      # Keep promotion behind CDS's all-images and immutable-tag gates below.
      tags: ""
    secrets:
      REGISTRY_USERNAME: ${{ github.actor }}
      REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}

  publish-images:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      packages: write
    needs: [get-version, build-and-test-images, attest-images]
    if: needs.get-version.outputs.publish == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Download tested digests
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: cds-digest-*
          path: digests
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check all release tags against tested digests
        env:
          VERSION: ${{ needs.get-version.outputs.version }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for name in go-dev-1.24-alpine; do
            digest="$(jq -er '.digest' "digests/$name.json")"
            [[ "$digest" =~ ^sha256:[0-9a-f]{64}$ ]]
            python3 .github/scripts/check_cds_tags.py \
              "$REGISTRY/$IMAGE_NAMESPACE/$IMAGE_PREFIX$name" "$digest" "$VERSION" "sha-$GITHUB_SHA" \
              > "digests/missing-$name"
          done

      - name: Promote tested digests to release tags
        env:
          VERSION: ${{ needs.get-version.outputs.version }}
        run: |
          # An older queued release must not move latest behind current main.
          git fetch --no-tags origin main
          current_version="$(git show FETCH_HEAD:cds-containers/VERSION | tr -d '[:space:]')"
          for name in go-dev-1.24-alpine; do
            image="$REGISTRY/$IMAGE_NAMESPACE/$IMAGE_PREFIX$name"
            digest="$(jq -er '.digest' "digests/$name.json")"
            published_tags=()
            while IFS= read -r tag; do
              published_tags+=("$image:$tag")
            done < "digests/missing-$name"
            if [[ "$VERSION" == "$current_version" ]]; then
              published_tags+=("$image:latest")
            fi
            if (( ${#published_tags[@]} > 0 )); then
              IMAGE="$image" EXPECTED_DIGEST="$digest" IMAGE_TAGS="$(printf '%s\n' "${published_tags[@]}")" \
                bash .github/scripts/promote-attested-image.sh
              echo "Published $image tags: ${published_tags[*]} ($digest)"
            else
              echo "Skipped $image; immutable tags already point to $digest"
            fi
          done

  summary:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    needs: [get-version, build-and-test-images, attest-images, publish-images]
    if: always()

    steps:
      - name: Build summary
        env:
          VERSION: ${{ needs.get-version.outputs.version }}
          VERSION_RESULT: ${{ needs.get-version.result }}
          BUILD_RESULT: ${{ needs.build-and-test-images.result }}
          ATTEST_RESULT: ${{ needs.attest-images.result }}
          PUBLISH_RESULT: ${{ needs.publish-images.result }}
          PUBLISH: ${{ needs.get-version.outputs.publish }}
        run: |
          {
            echo "## CDS Containers Build Summary"
            echo "Version: $VERSION"
            echo "Commit: $GITHUB_SHA"
            echo "Version validation: $VERSION_RESULT"
            echo "Build and smoke tests: $BUILD_RESULT"
            echo "Attestations: $ATTEST_RESULT"
            echo "Publication: $PUBLISH_RESULT"
          } >> "$GITHUB_STEP_SUMMARY"
          if [[ "$VERSION_RESULT" != "success" || "$BUILD_RESULT" != "success" ||
                ( "$PUBLISH" == "true" && "$PUBLISH_RESULT" != "success" ) ]]; then
            echo "Build, test, or publication did not complete successfully." >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
          if [[ "$PUBLISH_RESULT" == "success" ]]; then
            {
              echo "All 3 images passed smoke tests and were published. Usage:"
              echo '```yaml'
              echo "container:"
              echo "  image: $REGISTRY/$IMAGE_NAMESPACE/${IMAGE_PREFIX}tools:sha-$GITHUB_SHA"
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "All 3 images passed smoke tests. This run did not publish release tags." >> "$GITHUB_STEP_SUMMARY"
          fi
